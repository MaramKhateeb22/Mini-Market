import 'package:flutter/material.dart';

import 'widget/select_page_view_body.dart';

class SelectPageView extends StatelessWidget {
  const SelectPageView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SelectPageViewBody(

      ),
    );
  }
}
